<Window x:Class="Lab04.DashboardApp.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Lab04.DashboardApp"
        mc:Ignorable="d"
        Title="Dashboard" Height="450" Width="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Padding="10">
            <TabControl
                x:Name="WidgetsTabControl"
                        BorderThickness="0"
                        Background="Transparent">
            </TabControl>
        </Border>
        
        <Border Grid.Row="1" 
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         x:Name="InputTextBox"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         FontSize="13"
                         Margin="0,0,15,0"/>

                <Button Grid.Column="1" 
                        x:Name="SendButton"
                        Content="Send"
                        Height="35"
                        Padding="25,0"
                        FontSize="13"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand">
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
